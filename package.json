{
  "name": "playwright-chromium-aws-lambda-layer",
  "version": "1.0.0",
  "description": "AWS Lambda Layer with Playwright, using Chromium and the Serverless Framework",
  "scripts": {
    "build": "npm run clean && mkdir -p layer && cd layer && npm init -y && npm install --omit=dev --arch=x64 --platform=linux @sparticuz/chromium playwright-core && rm package*.json && cd ..",
    "clean": "rm -rf layer",
    "deploy": "serverless deploy"
  },
  "keywords": [
    "aws",
    "lambda",
    "layer",
    "playwright",
    "chromium",
    "serverless"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "serverless": "^4.25.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
